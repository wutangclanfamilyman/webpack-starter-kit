# webpack-starter-kit (ReactJS/Webpack)

Before starting work, you must install the modules
> npm install

To run the project use the command
> npm start

To build the project use the command
> npm run build